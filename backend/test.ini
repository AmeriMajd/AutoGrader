[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -ra --strict-config --strict-markers
markers =
    slow: tests that perform expensive PDF or image processing
    integration: tests using multiple real components